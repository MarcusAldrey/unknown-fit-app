{
  "name": "ecg",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:mobile": "pnpm --filter @ecg/mobile start",
    "dev:api": "apps\\api\\.venv\\Scripts\\python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir apps/api",
    "api": "pnpm run dev:api",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "pnpm@9.15.0"
}